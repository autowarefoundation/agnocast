# Resource index registration is handled by rclcpp_components_package_hook.
# agnocast_components_register_node writes to the same directory properties
# as rclcpp_components_register_node, so no additional registration is needed.
